#include<stdio.h>
main()

{
	
	float len,wid,area;
	printf("Enter len,wid of rectangle :");
	scanf ("%f%f",&len,&wid);
	
	area = len*wid;
	printf("Area of rectangle is : %.3lf",area);
	
	
}
